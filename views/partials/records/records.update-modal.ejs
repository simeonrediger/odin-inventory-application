<dialog class="modal" aria-labelledby="update-form-heading">
  <form
    name="update"
    method="post"
    <% if (locals.updateErrors?.length > 0) { %> data-invalid <% } %>
    class="modal-form"
  >
    <h2 id="update-form-heading">Edit record</h2>

    <label for="update-artist-id">Artist</label>
    <select name="artistId" id="update-artist-id" required>
      <option
        value=""
        <% if (!locals.updateFields?.artistId) { %> selected <% } %>
        disabled
        hidden
      >Select one</option>
      <% artists.forEach(artist => { %>
        <option
          value="<%= artist.id %>"
          <% if (artist.id == locals.updateFields?.artistId) { %>
            selected
          <% } %>
        ><%= artist.name %></option>
      <% }); %>
    </select>

    <label for="update-name">Name</label>
    <input
      type="text"
      name="name"
      id="update-name"
      value="<%= locals.updateFields?.name %>"
      required
    />

    <label for="update-price">Price</label>
    <input
      type="number"
      name="price"
      id="update-price"
      <% if (locals.updateFields?.price) { %>
        value="<%= centsToDollars(updateFields.price) %>"
      <% } %>
      min="0"
      step="0.01"
      required
    />

    <label for="update-quantity">Quantity</label>
    <input
      type="number"
      name="quantity"
      id="update-quantity"
      placeholder="0"
      value="<%= locals.updateFields?.quantity %>"
      min="0"
    />

    <label for="admin-password">Admin password</label>
    <input type="password" name="adminPassword" id="admin-password" required />

    <input type="hidden" name="returnTo" />

    <%- include('../form-errors', { errors: locals.updateErrors }) %>

    <div class="cancel-confirm-button-row">
      <button
        type="button"
        data-action="cancel-update"
        class="cancel-button"
      >Cancel</button>
      <button type="submit">Update record</button>
    </div>
  </form>
</dialog>
